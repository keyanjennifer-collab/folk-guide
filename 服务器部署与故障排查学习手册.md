# 五色知时：服务器部署与故障排查学习手册

这份手册记录一次完整的上线同步和故障排查过程，适用于 Ubuntu 服务器、Docker Compose v2、FastAPI 后端、PostgreSQL 数据库和微信小程序前端。

## 一、先记住项目结构

服务器项目目录：

```text
~/folk-guide
```

部署文件目录：

```text
~/folk-guide/deploy
```

主要服务：

| 服务 | 作用 |
| --- | --- |
| `api` | FastAPI 后端、起盘、合盘、AI 解读 |
| `db` | PostgreSQL 数据库 |
| `caddy` | HTTPS 和域名反向代理 |
| `migrate` | 执行 Alembic 数据库迁移的临时服务 |

数据库数据保存在 Docker volume 中。只要不执行 `docker compose down -v`，数据库数据就不会因为重新创建 API 容器而删除。

## 二、同步 GitHub 最新代码

在服务器执行：

```bash
cd ~/folk-guide && git pull --ff-only origin main
```

看到下面的结果表示服务器已经是远端最新提交：

```text
Already up to date.
```

`git pull` 被中断一般不会留下半个提交。Git 会在完整提交准备好后才更新分支指针，可以用下面的命令检查仓库：

```bash
cd ~/folk-guide && git status --short --branch && git rev-parse --short HEAD && git fsck --full --no-progress
```

`git fsck` 偶尔会显示 `dangling commit`。这通常只是没有分支引用的旧提交，不等于仓库损坏。只要当前分支和远端提交一致，就不需要删除它。

## 三、为什么拉了代码还要重新构建 API 镜像

`docker-compose.yml` 使用了：

```yaml
build:
  context: ..
  dockerfile: backend/Dockerfile
```

Dockerfile 会把 `backend/app` 和 `alembic` 复制进镜像。服务器上的 Git 文件更新后，旧镜像里的代码不会自动更新。

因此，更新后需要构建镜像：

```bash
cd ~/folk-guide/deploy && docker compose --env-file .env.production build api migrate
```

Docker 会尽量复用缓存。依赖没有变化时，通常不会重新下载 Python 包和 Node 包。

`--no-cache` 会强制从头下载和构建，日常更新不要使用。

## 四、推荐的后端更新命令

下面这行会同步代码、构建 API、执行数据库迁移，并只重新创建 API 容器：

```bash
cd ~/folk-guide && git pull --ff-only origin main && cd deploy && docker compose --env-file .env.production build api migrate && docker compose --env-file .env.production run --rm migrate && docker compose --env-file .env.production up -d --force-recreate api && docker compose --env-file .env.production ps
```

这条命令不会：

- 删除 `deploy-api` 镜像；
- 删除 PostgreSQL 容器中的数据；
- 删除任何 Docker volume；
- 强制重新下载没有变化的依赖。

不要在生产环境使用：

```bash
docker compose down -v
```

`-v` 会删除项目关联的数据卷，可能导致数据库数据丢失。

## 五、Docker Compose v1 和 v2

推荐使用 Compose v2：

```bash
docker compose version
```

正确示例：

```text
Docker Compose version 2.40.3+ds1-0ubuntu1~22.04.1
```

Compose v1 在重新创建旧容器时可能报：

```text
KeyError: 'ContainerConfig'
```

升级到 Compose v2 后使用：

```bash
docker compose --env-file .env.production up -d --force-recreate api
```

不要把 `docker compose` 和旧版 `docker-compose` 混用。

## 六、部署完成后的基础检查

先看服务状态：

```bash
cd ~/folk-guide/deploy && docker compose --env-file .env.production ps
```

通常应看到：

```text
deploy-api-1      Up
deploy-caddy-1    Up (healthy)
deploy-db-1       Up (healthy)
```

检查数据库、迁移版本、域名和字体：

```bash
cd ~/folk-guide/deploy && docker compose --env-file .env.production exec -T db pg_isready && docker compose --env-file .env.production exec -T api alembic current && curl -fsS -o /dev/null -w 'health: %{http_code}\n' https://api.wusezhishi.com/health && curl -fsS -o /dev/null -w 'api-docs: %{http_code}\n' https://api.wusezhishi.com/docs && curl -fsS -o /dev/null -w 'website: %{http_code}\n' https://wusezhishi.com && curl -fsS -o /dev/null -w 'font-sans: %{http_code}\n' https://api.wusezhishi.com/font-assets/wuse-sans.woff2 && curl -fsS -o /dev/null -w 'font-serif: %{http_code}\n' https://api.wusezhishi.com/font-assets/wuse-serif.woff2
```

正常结果类似：

```text
accepting connections
0008_daily_guidance_audit
health: 200
api-docs: 200
website: 200
font-sans: 200
font-serif: 200
```

`version is obsolete` 只是 Compose 文件中的旧 `version` 字段提示，不影响服务运行：

```text
the attribute `version` is obsolete, it will be ignored
```

## 七、查看 API 日志

持续查看最新日志：

```bash
cd ~/folk-guide/deploy && docker compose --env-file .env.production logs -f --tail=0 api
```

只筛选起盘、AI 和错误：

```bash
cd ~/folk-guide/deploy && docker compose --env-file .env.production logs -f --tail=0 api | grep --line-buffered -E 'ziwei|ai|ERROR|Traceback|500|502'
```

按 `Ctrl+C` 只会退出日志查看，不会停止容器。

常见结果：

| 日志 | 含义 |
| --- | --- |
| `POST /api/ziwei/charts ... 200 OK` | 起盘保存成功 |
| `POST /api/ziwei/compatibilities ... 200 OK` | 合盘保存成功 |
| `POST /api/ziwei/interpret ... 200 OK` | 单盘 AI 解读成功 |
| `POST /api/ziwei/compatibility/analyze ... 200 OK` | 合盘 AI 解读成功 |
| `500 Internal Server Error` | 后端代码、数据库或运行时错误 |
| `502 Bad Gateway` | 后端请求外部 AI 模型失败 |
| `503` 且提示模型未配置 | API 容器没有读到完整模型配置 |

## 八、起盘成功但 AI 解读失败时怎么判断

如果日志有：

```text
POST /api/ziwei/charts ... 200 OK
```

但没有：

```text
POST /api/ziwei/interpret
```

先检查小程序是否打开了正确项目、是否重新编译，以及是否在结果页执行了最新代码。

如果已经出现：

```text
POST /api/ziwei/interpret ... 502 Bad Gateway
```

说明请求已经到达后端，问题转移到了外部模型服务。此时不需要删除 API 容器或重建数据库。

## 九、小程序请求超时的原因

普通 API 请求可以在几秒内完成，但 AI 模型生成可能需要更久。项目最初的统一请求超时时间是 15 秒，而后端模型设置是：

```text
LLM_TIMEOUT_SECONDS=30
LLM_MAX_RETRIES=2
```

如果小程序显示：

```text
请求超时，请检查网络后重试
```

不一定代表服务器没有收到请求。后端访问日志通常要等请求结束后才打印。

正确做法是让 AI 请求单独使用更长的超时时间，普通接口仍保持较短超时。当前代码为单盘和合盘 AI 请求设置了 120 秒，并在 `api.ts` 中支持 `timeoutMs` 覆盖。

## 十、检查 AI 模型运行配置

下面的命令只显示密钥是否存在，不会输出密钥内容：

```bash
cd ~/folk-guide/deploy && docker compose --env-file .env.production exec -T api python -c 'from app.config import get_settings; s=get_settings(); print("LLM_API_KEY:", bool(s.llm_api_key)); print("LLM_BASE_URL:", s.llm_base_url); print("LLM_MODEL:", s.llm_model); print("LLM_TIMEOUT_SECONDS:", s.llm_timeout_seconds); print("LLM_MAX_RETRIES:", s.llm_max_retries)'
```

至少应满足：

```text
LLM_API_KEY: True
LLM_BASE_URL: https://api.deepseek.com
LLM_MODEL: deepseek-chat
```

如果修改了 `.env.production`，需要重新创建 API 容器才能让新环境变量生效：

```bash
cd ~/folk-guide/deploy && docker compose --env-file .env.production up -d --force-recreate api
```

修改环境变量通常不需要重新构建镜像，因为配置来自容器启动时的 `env_file`。

## 十一、DeepSeek 模型配置

DeepSeek OpenAI 兼容接口使用：

```text
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
```

曾经使用的：

```text
LLM_MODEL=deepseek-flash
```

会导致外部模型服务拒绝请求，后端把它统一转换成：

```text
502 Bad Gateway
紫微解读模型暂时不可用，请稍后重试
```

修改模型名称的安全操作：

```bash
cd ~/folk-guide/deploy && cp .env.production .env.production.bak && sed -i 's/^LLM_MODEL=.*/LLM_MODEL=deepseek-chat/' .env.production && docker compose --env-file .env.production up -d --force-recreate api && docker compose --env-file .env.production exec -T api python -c 'from app.config import get_settings; print(get_settings().llm_model)'
```

看到：

```text
deepseek-chat
```

再回到小程序重新进入结果页。

## 十二、每次更新的最短流程

日常只修改了后端代码时：

```bash
cd ~/folk-guide && git pull --ff-only origin main && cd deploy && docker compose --env-file .env.production build api migrate && docker compose --env-file .env.production run --rm migrate && docker compose --env-file .env.production up -d --force-recreate api
```

只修改了 `.env.production` 时：

```bash
cd ~/folk-guide/deploy && docker compose --env-file .env.production up -d --force-recreate api
```

只修改了微信小程序代码时：

1. 在微信开发者工具打开 `D:\\wuse\\folk-guide\\project.config.json`。
2. 确认 `miniprogramRoot` 是 `miniprogram/`。
3. 清除缓存。
4. 重新编译。
5. 用服务器日志确认请求是否到达。

## 十三、遇到问题时的判断顺序

按照下面顺序排查，可以避免反复重装：

1. `docker compose ps`：容器是否运行。
2. `pg_isready`：数据库是否可连接。
3. `alembic current`：数据库迁移是否到最新。
4. `curl /health`：API 是否健康。
5. API 日志：请求是否到达、返回什么状态码。
6. 小程序请求：是否使用了正确项目和最新编译结果。
7. 外部 AI 配置：密钥、地址、模型名、额度和外网连接。

只有确认是镜像代码旧时才重新 `build`；只有修改环境变量时只需 `--force-recreate`；不要把删除容器、删除 volume 当成日常更新步骤。

