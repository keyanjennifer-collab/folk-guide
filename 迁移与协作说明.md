# 五色知时小程序 · 迁移与协作

## 编辑与同步

本机唯一编辑目录：`/Users/hlyo/Desktop/五色知时/五色知时小程序`。微信开发者工具已导入此目录。旧的 `术数数据/folk-guide-main` 保留作迁移前备份，不再编辑。

共用仓库：https://github.com/keyanjennifer-collab/folk-guide 。协作前先拉取远端更新，有冲突时合并，不使用强制推送覆盖他人提交。用户已确认正式 AppID 为 `wx071d4f5e34e12c95`；后端部署时应同步配置对应的微信凭据。

同步源代码、品牌压缩素材、配置模板、数据库结构迁移与测试。GitHub 是代码仓库，不是订单数据库；真实账号、订单、生辰档案、密钥、本机日志和数据库文件不上传。双方各自在后端配置环境变量。

## 本版界面

- 首页：以 `COLORO 129-15-00`（屏幕近似 `#262626`）为主色的深色双栏、低对比岩面纹理、五色色序、选色联动香品、用色依据和礼盒入口。
- 今日五色：当日排行只读取后台人工确认并发布的资料。若当日未发布，可展示指定聊天中最近一次已确认资料，但必须同时显示原日期及“非今日排行”提示，不得由前端或 AI 补算。
- 商城：五色知时线香套装、青木、朱蜜、黄檀、白桂、墨沉。详情、数量调整和本机购物袋可用。
- AI 国学：保留真实后端问答、权益、历史、引用与反馈；游客需主动登录。
- 我的：微信登录入口、本人档案、个人五色、订单状态、问答权益/记录、使用帮助、隐私与退出。
- 订单：登录保护、本人列表、状态筛选、分页、详情和物流信息；服务端按账号隔离。
- 知时小趣：节气日常、循心选香和国学一题，不按年龄贴标签，兼顾独处与家人朋友共玩。

设计来源为项目负责人指定的「首页概念稿-深色交互版.html」。岩石纹理已压缩并内嵌为 `miniprogram/app.wxss` 中的全局材质变量，避免微信编译器错误解析本地 WXSS 图片路径。香调为概念稿文案，不作为已核验原料成分承诺。

## 测试

```sh
npm ci
npx tsc --noEmit
node tools/check-miniprogram.cjs
cd backend
python -m pytest tests/test_orders.py tests/test_user_account.py tests/test_public_guides.py tests/test_ai_chat.py tests/test_profile.py -q
```

底栏图标已提交，无需重生成。只有修改图标时才需要在开发环境中安装 sharp 并运行 `node tools/build-tab-icons.cjs`。

## 尚未开放的交易能力

当前商品定价为五色礼盒 268 元、单香 68 元；单支线香长度约 10.5 厘米，单个小盒净含量 15 克，五色礼盒净含量 75 克。商城代码已接入服务端计价、库存、地址、预购支付、订单、退款和后台发货；正式环境仍需完成真实微信支付、库存盘点、客服信息和合规材料配置。

生产发布前按 `deploy/README.md` 设置 HTTPS API、微信服务端凭据、数据库与模型配置，执行数据库迁移并真机验证微信登录。不要将开发工具的本机 API 地址作为生产地址发布。

本机 launchd 服务文件留在 backend 目录，但不提交到仓库；它包含个人电脑的绝对路径。其他协作者使用部署说明启动自己的后端。
